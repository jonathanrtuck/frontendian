import React from 'react';

import { Link } from 'gatsby';

const Header = () => (
    <header>
        <nav>
            <Link to="/">Frontendian</Link>
        </nav>
    </header>
);

export default Header;
