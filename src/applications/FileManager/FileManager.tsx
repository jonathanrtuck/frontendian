import { type Application } from "@/types";
import { type FunctionComponent, type ComponentProps } from "react";

export const FileManager: FunctionComponent<
  ComponentProps<Application["Component"]>
> = () => null;
