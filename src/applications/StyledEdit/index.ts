import { ApplicationConfiguration } from "@/types";

import { StyledEdit } from "./StyledEdit";
import { StyledEditIcon } from "./StyledEditIcon";

export const APPLICATION_STYLED_EDIT: ApplicationConfiguration = {
  Component: StyledEdit,
  Icon: StyledEditIcon,
  id: "application-styled-edit",
  title: "StyledEdit",
};
